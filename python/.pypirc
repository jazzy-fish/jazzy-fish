[distutils]
index-servers =
    pypi
    testpypi

[pypi]
repository: https://upload.pypi.org/legacy/
username = **token**
password = ${PYPI_PASSWORD}

[testpypi]
repository: https://test.pypi.org/legacy/
username = **token**
password = ${PYPI_PASSWORD}
